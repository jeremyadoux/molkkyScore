var browserIE = true;
var $scoreTableContainer = $('#scoreTable-container');
var $scoreTable = $('#mainTable #scoreTable');

$(document).ready(function(){ 
	$scoreTable.height($scoreTableContainer.height());
});